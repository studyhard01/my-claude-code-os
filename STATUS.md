# STATUS — 현재 진행 상태

> **살아있는 상태(現況) 문서.** `OS.md`가 "무엇을·왜 만들 것인가"(계획, 잘 안 변함)라면,
> 이 파일은 "지금 어디까지 왔나"(현황, 자주 변함)를 담는다. 세션·서브에이전트가 시작할 때
> 여기부터 보면 "지금 뭘 해야 하는지"를 즉시 안다. **작업이 끝나거나 막히면 이 파일을 갱신한다.**
>
> 갱신 규칙: 완료→✅로 옮기고, 새로 막히면 ⛔에 추가, "다음 할 일"을 항상 1~3개 유지.

- **최종 갱신**: 2026-07-19 (**PR #6 병합 + 위생 조각 완료.** 카탈로그 10종 병합, 만료 공고 수집 스킵 구현 — 잡알리오 라이브 실측 500건 중 445 스킵)
- **현재 마일스톤**: **M2 — 회사 구독 + 회사 리서치** (직무 범위: 개발직군 + 직무 미분류 공공 통합공채)
- **한 줄 요약**: **M2 조각 2개 소진.** 착수 조각(카탈로그 확장, PR #6 병합)과 위생 조각(만료 스킵) 완료. 다음: **M2a 회사 구독 3조각**(착수 시 12.9 계약 확정 Draft PR 부터) → M2b 회사 리서치 (순서는 OS.md 9장, 2026-07-17 확정).

---

## ✅ 완료

- 개발 실행 구조·계약 확정 (OS.md 12장)
- 협업 인프라: 공유 서브에이전트 3 + 스킬 5 + 훅 2 (상세 = README.md)
- 앱 스캐폴딩 + 공유 계약 `src/types/contract.ts`
- 4화면 UI: 온보딩 / 피드 / 상세 / 저장
- 실 DB 읽기(Prisma + SQLite), 북마크 CRUD, `role` 다중값 필터
- API 엔드포인트 8종 (GET /api/jobs … DELETE /api/bookmarks/:id)
- 컨텍스트 보강: CLAUDE.md 항상-참인 사실 주입
- **`status-context` 훅(SessionStart)** — 이 파일(`STATUS.md`)을 매 세션 자동 주입. "먼저 확인하라"는 부탁을 보장으로 전환. `jq` 없이 `sed`만 사용
- **`contract-context` 훅(SubagentStart)** — `src/types/contract.ts` 전문을 개발 에이전트(backend/frontend)에만 주입. payload 의 `agent_type` 으로 분기(실측 확인)
- **`decision-log` 훅 복구** — `jq` 의존 제거(`sed` 파싱), 스냅샷 diff 로 **바뀐 절 이름**까지 기록, 파싱 실패 시 `.claude/decision-log.err` 에 흔적. 2026-07-05 이후 조용히 죽어 있던 것을 살림
- **`skill-usage-log` 훅 복구·등록** — `jq` 의존 제거, JSON 상태 파일 → append 전용 로그(`.claude/skill-usage.log`)로 전환, 기록 위치를 프로젝트 안으로 이동, `settings.json` 에 등록. `skill-stat` 도 `awk` 집계로 재작성 (**훅 6개, 실행 코드에 `jq` 0곳**)
- **컨텍스트 설계 문서 `docs/context.md`** — 도식(mermaid), 훅 5개 설명, 설계 원칙. **A/B 테스트로 계약 주입 효과 실측**: 정답률은 동일하나 도구 호출 2→0, 토큰 −14%, 시간 −22%. 실험 중 "SubagentStart 훅은 같은 세션에서 바로 돈다"는 사실을 발견(PreToolUse 는 안 돎)
- **피드 정렬·필터 UX 개선** (orchestrate 시연) — `sort=recent` 정렬·집계 규약 구현(`totalCount` 부분집합 버그 수정), 필터↔URL 동기화(새로고침·공유 복원), 적용된 필터 칩·"필터 전체 해제". 계약 모호점을 OS.md 12.6/6장에 명문화(DECISIONS.md 자동 기록)
- **수집 파이프라인 구현 (OS.md 12.8)** — fixture(`saramin-job-search.json`, FULL 5+PARTIAL 4) → `SaraminAdapter`(fetchFn 주입, 실행당 최대 5콜) → `Normalizer`(name 키워드 라벨 매핑·dedupKey·dataQuality) → `scripts/collect.ts`(`COLLECT_SOURCE` 스위치, idempotent upsert). `saramin-fixture` 모드로 수집→upsert→`GET /api/jobs` 노출까지 검증(재실행 시 신규 0 확인)

- **루프 지도(`docs/dev-loop-map.html`) + 자동 갱신 훅 `loop-map-context`** — 개발 루프를 사람·AI·CI 3레인으로 시각화(병목/반복/낭비 진단·변경 이력 포함). 루프 구성 파일(훅·스킬·settings·CI)이 수정되면 훅이 "지도 갱신·재발행" 컨텍스트를 자동 주입(**훅 스크립트 6개**, 등록 당일 같은 세션에서 동작 실측). 발행 주소는 지도 파일 머리 주석에
- **`/retrospect` + `LESSONS.md`** — 세션(루프) 끝에 "다음 세션의 행동을 바꾸는 지식"만 누적(세션당 최대 5줄, 항상 필요한 건 CLAUDE.md 승격). ralph-test 종료 시 자동 실행. 루프 지도의 **반복 ① 해소** — 최초 진단 6건 중 4건 해소, 남은 건 병목 2(사람 승인=유지, 사람인 API=외부)
- **루프 진단 후 낭비 2건 해소** — ① CI 도입(`.github/workflows/ci.yml`: push/PR 마다 db:push→typecheck→test. 원격=백업→검문소) ② `/commit` 스킬에 검증 게이트("초록불 없이 커밋 금지", 문서-only 커밋만 예외). 홈(`~/.claude`)·프로젝트 두 commit 스킬 동기화. **CI 첫 실행 성공 확인(2026-07-13, run #1, 32초)** — 검문소 가동
- **`ralph-test` 랄프 루프 스킬 + Vitest 도입** — "한 호출 = 한 바퀴(모듈 1개 테스트→통과 확인→커밋→기록)" 구조, 반복은 `/loop /ralph-test` 가 담당. 진행 상태 = `.claude/ralph-test-progress.md`(대상 8건, 우선순위순). 첫 바퀴(Normalizer 15개 테스트)로 절차 검증 완료
- **push 게이트 `/ship` + `npm run eval` (2026-07-14)** — "push 는 사람이 아니라 평가가 허가한다". rubric 11항목(`docs/eval-rubric.md`): 정적 검증(typecheck·test) + 기동 + 기능 API 왕복 8종(피드·정렬·필터·상세·404 계약·북마크 CRUD·사용자 조건) + 응답 시간(목록 평균<500ms 등). 전 항목 통과(100점)일 때만 push. 평가 서버는 3100 포트 분리. 첫 실측 100/100(목록 평균 25ms·상세 68ms). 루프 지도 **병목 ① 의 push 부분 해소**(승인은 안전장치로 유지). 부수 수확: C3 필터 검사의 "빈 배열이면 무조건 통과" 함정을 발견해 실존 role + 1건 이상으로 강화
- **계약 승인 게이트 (2026-07-14)** — `/orchestrate` 에 2단계 신설: 계약(OS.md 12장)이 바뀌는 작업만 **Draft PR** 로 계약 diff 를 만들어 구현 전에 사람이 승인(반려 시 기획으로 복귀, 승인 시 같은 PR 에 구현 커밋 누적 → /ship 통과 후 Ready 전환). 계약 무변경 작업은 건너뜀 — 게이트 남발로 루프를 되돌리지 않기 위한 조건부 설계. 루프 지도 8→9단계 재편(handoff 참조 번호 동기화)

- **수집 다변화 계약 (2026-07-14, PR #2)** — 결정 3건: ① M1 제2 소스 추가 ② M2 축 = 회사 구독 라이트(12.9 계약 초안) ③ 채용 플랫폼 크롤링 금지 명문화(잡코리아 v 사람인 대법 판례). RawJob 정규 필드 승격(12.8 확정). **계약 승인 게이트(Draft PR) 첫 실전 — 기획→Draft PR→사용자 승인→구현 순서가 실제로 돌았다**
- **제2 소스 = 잡알리오로 확정 + 어댑터 구현 (2026-07-14, PR #3)** — 처음엔 워크넷 채용정보목록을 골랐으나 **개인 발급 불가**(민간 직업정보제공기관 자격+사업자등록증 필요) 판명 → 폐기. 대체로 **잡알리오(재정경제부 공공기관 채용, data.go.kr 15125273)**: 개인 개발키 자동승인·실호출 성공(전 11.2만건/정보통신 8.5천건), `AlioAdapter`(`/recruitment/list` JSON, 실응답 필드 그대로 매핑, `ncsCdLst=R600020` 개발 필터, 본문 결합으로 **description 채움** — 사람인 약점 보완), fixture 6건, `COLLECT_SOURCE=alio[-fixture]`, 테스트 30개·평가 게이트 100/100. **정직한 제약**: 잡알리오 NCS직무는 "정보통신" 같은 대분류라 우리 세부 직군에 거의 안 붙음 → 대부분 `jobRole=null → PARTIAL`. (**정정 2026-07-16**: 여기 "피드 기본 숨김"이라고 적혀 있었으나 **사실과 다르다** — OS.md 12.6 계약상 PARTIAL 은 *필터 축이 null 이라 가려질 때만* `partialHiddenCount` 로 빠진다. **필터를 안 건 기본 피드에선 그대로 노출된다**. 실측: 기본 피드 `partialHiddenCount=0`.) 잡알리오 가치는 "역할 필터 공고"가 아니라 **본문·원문URL**. (계약 미변경·M2 세분화 과제로 이관.) srcUrl 정크(~26%) 폴백은 검증된 ALIO 목록 페이지로(깊은링크 404 실측 후 교정)

- **ralph-test 루프 완주 (2026-07-14, `/loop /ralph-test` 7바퀴 연속 자동)** — 대상 9건 전부 소진, **테스트 91개**(normalizer·format·serialize·saramin/alio 어댑터·api·jobs/bookmarks 라우트·collect). 산출 인프라: 테스트 전용 `prisma/test.db` 격리(`tests/setup/`, 실 dev.db 무접촉), 스크립트는 tsx 자식 프로세스 검증. 헛바퀴 0, 게이트가 잡은 오류 1(4바퀴 typecheck). 종료 후 /retrospect 자동 수확

- **잡알리오 라이브 수집 + M1 완료 기준 검증 (2026-07-14)** — 실 API 500건 수집·적재(신규 500/갱신 0), **전건 본문·마감일 보유**(사람인 약점 보완 실증), 마감 전 61건 피드 노출. 평가 게이트 100/100 으로 "온보딩→피드→북마크" 실 데이터 완주 확인(총 74건 피드, 목록 평균 7ms). 라이브 실측 확보: FULL 비율 0.8%(NCS 대분류 제약), 만료 공고 다수 유입 → M2 과제로 이관

- **3주차 루프 엔지니어링 문서 `docs/loop.md` (2026-07-15)** — 2주차 `docs/context.md` 와 같은 형식으로 이번 주에 만든 루프를 문서화: 한 바퀴 스킬(ralph-test)·반복기(/loop)·기억(진행 파일 📚·LESSONS.md)·게이트(commit/ship/CI/계약 승인)·지도(dev-loop-map) 다섯 부품 + 설계 원칙 4가지. README 주차별 문서 표에 링크 추가. /ship 게이트 100/100 통과 후 push, CI 초록불(44초)

- **PR #5 병합 — 통합공채 펼침 구획 + push-gate 훅 (2026-07-17)** — ① **계약**: `role` 예약 토큰 `unassigned` 신설(12.5·12.6) — `jobRole IN (...) OR jobRole IS NULL`, 대상은 오직 `jobRole=null`(`partialOnly` 불리언은 "이름이 거짓말"이라 기각). ② **backend**: route.ts 최소 diff + 테스트 5개(총 128). 라이브 검증: 단독 70건 전부 null, `backend,unassigned`=78, 회귀 없음. ③ **frontend**: `UnassignedSection` — role 필터 활성 시 넛지를 펼침 가능하게, 결과 아래 별도 구획("직무 미분류 — 내 필터로는 확인 불가"), 필터 변경 시 자동 접힘. 덤: PARTIAL 카드 CTA "사람인에서 직접 확인" 하드코딩 버그 발견·수정(잡알리오·카카오 공고에 거짓 출처 라벨). ④ **push-gate 훅**(PreToolUse Bash, 사용자 승인): force push=deny, 평가 표식(`.claude/.eval-pass`, evaluate.ts 가 PASS 때 기록) 15분 내=allow, 그 외 push=ask — CLAUDE.md 의 push 부탁 2건이 기계 보장으로. "macOS=BSD, GNU 전용 옵션 금지" 교훈 CLAUDE.md 승격. `npm run lint` 함정은 스크립트째 삭제(규칙이 필요 없게)
- **PR #4 병합 — 수익화 소스 재편 + 카카오 어댑터 + 훅 복구 (2026-07-17)** — ① **계약**: 사람인 = 수익화 불가 확정(약관상 대가 수취 금지가 **서비스 단위**로 걸려 '도구 과금'으로도 회피 불가) → 무료 티어 전용 격리. 수익화 코어 = 잡알리오(상업 자유 명시) + 회사 공식 페이지 계열(카카오 → 그리팅 M3). 원티드 = 서면 확인 전 금지. ② **KakaoCareersAdapter**(12.8(6)) — 라이브 28건, **FULL 17.9%**(잡알리오 0.8%의 22배), **description 100%**, 직무 매핑 50%. **직무 붙은 공고 10 → 24건**. ③ **`decision-log` 훅 복구** — GNU 전용 diff 옵션을 써서 **macOS(BSD diff)에서 또 조용히 죽어 있었다**(자기 헤더에 "조용히 실패하지 않는다"고 적어둔 그 실패를 재현). `-U0`+awk 로 교체, 복구 즉시 밀린 변경을 절 이름까지 따라잡아 기록. ④ 중재 수확: backend 가 "목표 미달성"이라 보고했으나 **측정 지점이 틀렸다**(무필터 피드는 주 경로가 아님 — 온보딩 조건이 초기 필터). 정렬 규약 개정 3안 전부 기각, **진짜 병목은 잡알리오 수집 품질**로 재지정. 테스트 91 → 123개
- **M2 위생 조각 — 수집 시 만료 공고 스킵 (2026-07-19)** — 피드(12.6)와 **같은 경계**(오늘 UTC 자정 이전 마감 = 만료, 오늘 마감·상시는 유지)를 `src/lib/collect/expiry.ts` 로 분리, upsert 전에 거른다(기존 만료 행 삭제는 안 함 — 피드가 어차피 숨김). **잡알리오 라이브 실측: 500건 수신 → 만료 스킵 445, 마감 전 55건만 적재**(신규 10/갱신 45). `COLLECT_NOW` 주입으로 테스트 시간 고정 — fixture 마감일이 지나면 저절로 깨지는 구조(C3 사고 유형)를 설계 단계에서 차단. 테스트 133→141개. 부수: 저장소의 실질 main = `studyhard01` 브랜치임을 실측(repo main 은 스터디 공용)·CLAUDE.md 에 규칙 승격
- **M2 PR #1 — 직무 카탈로그 확장 구현 (2026-07-19, PR #6 병합)** — 계약(12.10, 사용자 승인) 구현: `DEV_ROLE_OPTIONS` 7→10종(`ai-ml`·`robotics`·`qa`) + `data` 라벨 "데이터 엔지니어" 정정, ML 키워드 data→ai-ml 재귀속. 짧은 토큰("ai"·"qa"·"slam")은 **단어 경계(\b) 매칭 분리**로 email/qatar 오탐 방지(오탐 테스트 동봉). **완료 기준 전부 실측 충족**: 카카오 라이브 재수집 매핑 **14→26/28**(기준 20), 신규 role API 실데이터 ai-ml 8·robotics 7·qa 1건, 회귀 0(null 14→2 — 남은 2건은 계약이 예상한 게임 기술PM·3D 렌더링), 테스트 128→133개, eval 100/100. 미매핑 공고는 `unassigned` 구획으로 도달(PR #5). **push-gate 훅 첫 실전 발화 확인** — 평가 표식 15분 내 push 가 확인 요청 없이 allow(err 파일 없음·push 허용 규칙 없음으로 교차 검증). 잔여 1: 온보딩 칩 10개 실브라우저 스모크(Chrome 확장 미연결로 보류 — `.chips` flex-wrap 정적 확인으로 갈음)
- **lint 함정 제거 + 평가 게이트 C3 수정 (2026-07-16)** — ① `npm run lint` 삭제: eslint 설정·설치가 없어 `next lint` 는 검사 대신 되묻기만 했다(사람은 멈추고 에이전트는 실패). CLAUDE.md 의 "쓰지 말 것" 금지 규칙은 함정 위의 표지판이라 매 세션 컨텍스트를 먹었으므로, **함정을 없애 규칙째로 삭제**. ② **C3 가 시간이 지나면 저절로 깨지는 구조였다** — 피드 1페이지에서 role 있는 공고를 골라 필터를 검사했는데, 마감임박순 정렬이라 role 있던 공고가 만료돼 밀려나자 잡알리오 PARTIAL 20건이 1페이지를 채워 **필터가 멀쩡한데 FAIL**(95/100). 계약의 직무 7종을 순회하며 실제로 필터해 보는 방식으로 교체(날짜 무관 + API 오류도 잡아 이전보다 강함). rubric 이력 갱신. 부수 수확: STATUS 의 "PARTIAL 피드 기본 숨김" 서술이 **사실과 달랐음**을 실측으로 발견해 정정

## 🚧 진행 중 · 남은 것

> **2026-07-16 정정 — 이 절은 OS.md 와 모순이었다.** "사람인 승인 후 전환"만 남았다고 적혀 있었으나,
> OS.md 12.7 의 M1 완료 기준("온보딩→피드→북마크가 끝까지 도는 것")과 9장 소스 요구("1~2개")는
> **2026-07-14 잡알리오 라이브로 이미 충족·검증(100/100)** 됐다. 같은 파일이 "완료 기준 통과"와
> "사람인 대기만 남음"을 동시에 주장하고 있었다. **사람인은 완료 조건이 아니라 커버리지 보강이다.**

- **위생 조각 PR 병합 대기** — `m2/collect-skip-expired` 브랜치, 게이트 통과 후 push·PR 생성. 병합되면 M2a 로.
- **(사람인 승인 시) 무료 티어 소스로 편입** — `COLLECT_SOURCE=saramin` + `SARAMIN_ACCESS_KEY`, 실응답 필드명·구조 최종 검증(12.8). **M1 완주를 여기 묶지 않는다** — 무기한 외부 대기인 데다 수익화 시 제거 대상이다(PR #4 계약).
- **온보딩 칩 10개 실브라우저 스모크** — 2026-07-19 세션은 Chrome 확장 미연결이라 코드 정적 확인(`DEV_ROLE_OPTIONS.map` 직접 렌더 + `.chips` flex-wrap)으로 갈음. 다음에 브라우저 되는 세션에서 1회 눈으로 확인.

## ⛔ 블로킹 · 대기

- **M1 을 막는 것은 현재 없다** (2026-07-16). 카카오 어댑터는 승인 대기 없이 지금 진행 가능.
- **사람인 개발자센터 이용신청 → 승인 대기 중** — **M1 블로킹에서 내림**(2026-07-16). 잡알리오 라이브로 완료 기준이 이미 충족됐으므로 사람인은 "오면 얹는" 보강 소스다.
  - 승인 후 제약: 하루 500콜 / 요청당 count ≈ 110.
  - **수익화 불가**(2026-07-15 리서치, oapi.saramin.co.kr/caution): 약관상 대가 수취 금지가 **서비스 단위**로 걸려 '도구 기능만 과금' 모델로도 회피 불가 → 유료화 시 제거 또는 서면 제휴. 상세는 PR #4 계약(OS.md 7장).

## ▶️ 다음 할 일 (우선순위)

1. **위생 조각 PR 병합** (CI 확인 후)
2. **M2a 조각 ① — Company 엔티티 + 회사-공고 연결** — 착수 시 12.9 계약 확정 Draft PR 부터(2026-07-17 확정 절차). 그 뒤 ② 구독 CRUD + 피드 필터 ③ 온보딩 관심 회사 단계.
3. (M2a 진행 중 병렬 가능) 사람인 승인 오면 무료 티어 편입, 브라우저 되는 세션에서 온보딩 칩 10개 스모크.

> (해소된 기존 항목) "직무 카탈로그 확장" = PR #6 으로 완료. "잡알리오 coarse 직무 채택" = 기각(`unassigned` 구획으로 갈음, 9장). "무필터 피드 1페이지 정돈·직무 추정 분류" = M2 후반 후보(9장), dataQuality 세분화·네이버 어댑터·**사용자 행동 지표(라이트, 2026-07-19 사용자 결정 — 평가 3층: 기술 품질/기능 가치에 이어 "실제로 유용한가")**와 함께 대기.

## 관련 문서

- 계획·계약: `OS.md` (특히 9장 로드맵, 12장 개발 구조)
- OS 구성: `README.md` · `CLAUDE.md`
- 결정 변경 이력(자동): `DECISIONS.md`
