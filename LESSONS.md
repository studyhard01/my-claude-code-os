# 세션 교훈 누적 (LESSONS.md)

> `/retrospect` 스킬이 세션(특히 루프) 끝에 "다음 세션의 행동을 바꾸는 지식"을 여기에 쌓는다.
> 기록 규칙: 이슈 → 확인된 개선책이 한 줄에 드러나게. 세션당 최대 5줄, 수확 없으면 0줄.
> 겹치면 새 줄 대신 기존 항목 보강+날짜 갱신. 모든 세션에 항상 필요한 교훈은 CLAUDE.md 로 승격(여기선 삭제).
> 루프 한정 교훈은 각 루프의 진행 파일(예: `.claude/ralph-test-progress.md` 📚)이 담당 — 중복 금지.

## 2026-07-17 (수익화 소스 재편 — 카카오 어댑터·계약 게이트 2회전)

- **"동작 안 한다"를 판정하기 전에 그게 실사용자가 보는 화면인지부터 확인하라.** backend 가 무필터 피드를 보고 "카카오가 안 뜬다 → 목표 미달성"이라 보고했으나 주 경로는 **온보딩 조건이 초기 필터로 걸린 피드**였다(`presetFromPreference`). 하마터면 정렬 규약을 고쳐 증상만 가리고 진짜 원인(잡알리오 수집 품질)은 남길 뻔했다. 같은 계열로 **문서 서술도 실측과 어긋난다** — STATUS 의 "PARTIAL 피드 기본 숨김"은 정반대였고 M2 과제 증상도 거꾸로 적혀 있었다. 적힌 증상을 근거로 작업을 시작하기 전에 실측할 것.
- **검사가 "그때그때 데이터 표본"에 기대면 코드를 안 건드려도 날짜만 넘어가면 깨진다.** eval C3 가 피드 1페이지에서 role 있는 공고를 골라 썼는데 그 공고가 만료돼 밀려나자 **필터는 멀쩡한데 게이트가 FAIL** 했다 → 계약 카탈로그(`DEV_ROLE_OPTIONS`)를 순회하는 **고정 축**으로 교체. 표본이 아니라 계약을 기준으로 검사할 것.
- **서브에이전트에 긴 파일 편집을 시키지 말고 "판단만" 받아 메인이 적용하라.** product-planner 가 OS.md 를 편집하다 **12분·90k 토큰에 API 에러로 사망**했다(저장 전이라 파일은 무사). "도구 쓰지 말고 붙여넣을 완성 문구만 내라"로 바꾸니 **21k 에 완주**. 편집 루프는 길고 죽기 쉽다 — 판단은 위임하고 타이핑은 회수한다.
- **"쓰지 말 것" 규칙을 훅으로 강제하기 전에, 함정 자체를 없앨 수 있는지 먼저 보라.** `npm run lint` 금지 규칙은 eslint 미설치라 되묻기만 하는 스크립트 위에 세운 표지판이었고 **매 세션 CLAUDE.md 컨텍스트를 먹었다** → 스크립트를 지우니 규칙째 사라졌다. 규칙을 지키게 만드는 것보다 **규칙이 필요 없게 만드는 게 싸다**.

## 2026-07-14 (원격 강제 덮어쓰기 2차 — 다세션 협업 규칙)

- 원격이 강제 덮어쓰기로 갈라졌을 때 처방은 상황마다 다르다: **"서로 뭘 갖고 있나"부터 확인**(`git cherry origin/브랜치 HEAD` 로 patch 단위 대조). 내용이 같으면 rebase --onto(07-13 케이스), 원격이 내 커밋을 다 품고 앞서 있으면 `reset --hard origin/브랜치`(07-14 케이스).
- 강제 덮어쓰기가 이틀 연속 사고를 냈다 → **force push 금지, 브랜치 재구성은 PR 병합으로만** 규칙을 CLAUDE.md 로 승격(2026-07-14 합의).
- `reset`/`pull` 로 프로젝트 스킬이 갱신되면 **홈(`~/.claude`) 그림자 사본도 낡는다** — git 동기화 직후 홈 사본 diff 확인을 습관으로.
- **테스트 인프라가 구축돼 있다** (ralph-test 루프 산출): DB 필요하면 `tests/setup/` 의 test.db 격리(실 dev.db 무접촉), import 즉시 실행되는 스크립트는 리팩터링 말고 tsx 자식 프로세스로 실행해 검증 — 상세 패턴은 `.claude/ralph-test-progress.md` 📚.
- zsh 에서 `status` 는 **읽기 전용 예약 변수** — 셸 스크립트(훅·Monitor)에서 변수명으로 쓰면 즉사한다(실측). `run_state` 처럼 우회할 것.

## 2026-07-13 (루프 엔지니어링 시작 — CI·게이트·지도 자동화)

- 스킬·설정 파일이 홈(`~/.claude`)과 프로젝트 양쪽에 있으면 **홈 쪽이 그림자처럼 로드**될 수 있다 → OS 파일을 고치기 전에 양쪽 존재를 `diff` 로 확인하고, 고치면 둘 다 동기화(장기적으론 한쪽 제거).
- 원격이 강제 덮어쓰기(forced update)로 갈라졌을 때: 병합 방법을 고민하기 전에 **내용 diff 부터** — 차이가 0이면 `git rebase --onto origin/<브랜치> <옛 base>` 로 내 커밋만 옮기면 끝난다.
- `gh` 인증이 죽어도(401) 공개 저장소는 **무인증 REST API**(`api.github.com/repos/.../actions/runs`)로 CI 상태를 조회할 수 있다. 브라우저 확장·재로그인을 기다릴 필요 없음.
- settings.json 에 **새로 등록한 PostToolUse 훅이 같은 세션에서 바로 동작했다**(실측: loop-map-context). "훅은 세션 시작 스냅샷"이라고 단정하지 말고, 만들었으면 일단 매칭 파일을 건드려 실측할 것.
